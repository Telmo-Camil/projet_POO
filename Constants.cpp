#include "Constants.h"

const int cellSize = 10;    // Taille des cellules pour un meilleur affichage
const int gridWidth = 10;   // Largeur définie dans le fichier (nombre de colonnes)
const int gridHeight = 10;   // Hauteur définie dans le fichier (nombre de lignes)

