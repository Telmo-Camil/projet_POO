#include "Constants.h"

const int cellSize = 20;    // Ajustez la taille des cellules pour un meilleur affichage
const int gridWidth = 10;   // Largeur définie dans le fichier (nombre de colonnes)
const int gridHeight = 5;   // Hauteur définie dans le fichier (nombre de lignes)

